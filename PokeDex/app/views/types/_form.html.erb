<%= stylesheet_link_tag 'pokemons', media: 'all', 'data-turbolinks-track' => true %>

<%= form_for(@type) do |f| %>
  <% if @type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
      <% @type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :clase %>
  </div>



  <div class="actions">
    <%= f.submit "Crear Tipo", class: "btn" %>
  </div>
<% end %>
