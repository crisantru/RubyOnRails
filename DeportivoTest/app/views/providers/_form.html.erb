<%= form_for(@provider) do |f| %>
  <% if @provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@provider.errors.count, "error") %> prohibited this provider from being saved:</h2>

      <ul>
      <% @provider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="forms">
    <table class="table">
        <tr>
            <td> <%= f.label 'Nombre', for: 'text', id: "label" %> </td>
            <td> <%= f.text_field :name %> </td>
        </tr> 
        
        <tr>
            <td> <%= f.label 'Ciudad', for: 'text', id: "label" %> </td>
            <td> <%= f.text_field :city %> </td>
        </tr>  
        
        <tr>
            <td> <%= f.label 'Estado', for: 'text', id: "label" %> </td>
            <td> <%= f.text_field :state %> </td>
        </tr> 
        
        <tr>
            <td> <%= f.label 'Dirección', for: 'text', id: "label" %> </td>
            <td> <%= f.text_area :address %> </td>
        </tr>         
        
        <tr>
            <td> <%= f.label 'Teléfono', for: 'text', id: "label" %> </td>
            <td> <%= f.phone_field :phone %> </td>
        </tr>         
        
        <tr>
            <td> <%= f.label 'Email', for: 'text', id: "label" %> </td>
            <td> <%= f.email_field :mail %> </td>
        </tr> 
        
    </table>
</div>
    <hr>
  <div class="actions">
    <%= f.submit 'Envíar', class: 'button' %>
  </div>
<% end %>
