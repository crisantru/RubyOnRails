<%= form_for(@staff) do |f| %>
  <% if @staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@staff.errors.count, "error") %> prohibited this staff from being saved:</h2>

      <ul>
      <% @staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
      <hr>
      <div class="forms">
          
         <table class="table">
            <tr>
             <td><%= f.label 'Nombres', for: "text", id: "label" %></td>
             <td><%= f.text_field :name, id: "name" %></td>
            </tr>
      
             <tr>
             <td><%= f.label 'Apellidos', for: "text", id: "label" %></td>
             <td><%= f.text_field :last_name, id: "last_name" %></td>
            </tr>
             
             <tr>
             <td><%= f.label 'Direccion', for: "text", id: "label" %></td>
             <td><%= f.text_area :address, id: "address" %></td>           
            </tr>
             
            <tr>
                <td><%= f.label 'Telefono', for: "text", id: "label" %></td>
                <td><%= f.text_field :phone, id: "phone" %></td>
            </tr>
             
            <tr>
                <td><%= f.label 'email', for: "text", id: "label" %></td>
                <td><%= f.text_field :email, id: "email" %></td>
            </tr>
             
            <tr>      
              <td><%= f.label 'Tipo de Empleado', for: "text" , id: "label" %></td>
              <td><%= f.select :type_id, @lista_tipos %></td>
            </tr>
                        
            <tr>
                <td><%= f.label 'Sucursal', for: "text", id: "label" %></td>
                <td><%= f.select :branch_id, @lista_sucursales %></td>

            </tr>
      
         </table>
        </div>
      <hr>

  <div class="actions">
    <%= f.submit 'Envíar', class: 'button' %>
  </div>
<% end %>
